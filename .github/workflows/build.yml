name: Build and Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  windows-build:
    runs-on: windows-latest
    env:
      SCCACHE_DIR: ${{ github.workspace }}/.sccache
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup sccache cache
      uses: actions/cache@v4
      with:
        path: ${{ github.workspace }}/.sccache
        key: ${{ runner.os }}-sccache-${{ hashFiles('src/**.[ch]pp', 'src/CMakeLists.txt', 'src/**/CMakeLists.txt') }}
        restore-keys: ${{ runner.os }}-sccache-

    - name: Set up MSYS2 MinGW64
      uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        update: true
        install: >-
          mingw-w64-x86_64-gcc
          mingw-w64-x86_64-ninja
          mingw-w64-x86_64-cmake
          mingw-w64-x86_64-pkg-config
          mingw-w64-x86_64-sccache
          mingw-w64-x86_64-glew
          mingw-w64-x86_64-SDL2
          mingw-w64-x86_64-SDL2_mixer
          mingw-w64-x86_64-libpng
          mingw-w64-x86_64-libjpeg-turbo
          mingw-w64-x86_64-openal
          mingw-w64-x86_64-libvorbis
          mingw-w64-x86_64-flac
          mingw-w64-x86_64-mpg123
          mingw-w64-x86_64-opus
          mingw-w64-x86_64-wavpack
          mingw-w64-x86_64-zlib
          mingw-w64-x86_64-freetype
          mingw-w64-x86_64-enet
          flex
          lemon
          bison

    - name: Configure CMake
      shell: msys2 {0}
      run: |
        mkdir -p src/build-windows
        cd src/build-windows
        cmake -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_C_COMPILER_LAUNCHER=sccache \
          -DCMAKE_CXX_COMPILER_LAUNCHER=sccache \
          ..

    - name: Build
      shell: msys2 {0}
      run: |
        cd src/build-windows
        ninja -j4

    - name: Run tests
      shell: msys2 {0}
      run: |
        cd src/build-windows
        ctest --output-on-failure

    - name: sccache stats
      if: always()
      shell: msys2 {0}
      run: sccache --show-stats

    - name: Build legacy.wad
      shell: msys2 {0}
      run: bash src/build_wad.sh src/release/legacy.wad

    - name: Gather release files
      shell: msys2 {0}
      run: |
        mkdir -p src/release
        cp src/build-windows/doomlegacy.exe src/release/
        ldd src/build-windows/doomlegacy.exe | grep '/mingw64/' | awk '{print $3}' | xargs -I{} cp {} src/release/

    - name: Package release
      shell: msys2 {0}
      run: |
        cd src/release
        zip -r ../doomlegacy-windows.zip .

    - name: Upload Windows release
      uses: actions/upload-artifact@v4
      with:
        name: doomlegacy-windows
        path: src/doomlegacy-windows.zip
        retention-days: 30
        if-no-files-found: error

  linux-build:
    runs-on: ubuntu-latest
    env:
      SCCACHE_DIR: ${{ github.workspace }}/.sccache
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup sccache cache
      uses: actions/cache@v4
      with:
        path: ${{ github.workspace }}/.sccache
        key: ${{ runner.os }}-sccache-${{ hashFiles('src/**.[ch]pp', 'src/CMakeLists.txt', 'src/**/CMakeLists.txt') }}
        restore-keys: ${{ runner.os }}-sccache-

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          cmake ninja-build pkg-config sccache \
          gcc g++ \
          libsdl2-dev libsdl2-mixer-dev \
          libglew-dev \
          libpng-dev libjpeg-dev \
          zlib1g-dev \
          libopenal-dev \
          libvorbis-dev libflac-dev libmpg123-dev libopus-dev libopusfile-dev \
          libfreetype-dev \
          libenet-dev \
          libwavpack-dev \
          flex bison lemon

    - name: Configure CMake
      run: |
        mkdir -p src/build-linux
        cd src/build-linux
        cmake -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_C_COMPILER_LAUNCHER=sccache \
          -DCMAKE_CXX_COMPILER_LAUNCHER=sccache \
          ..

    - name: Build
      run: |
        cd src/build-linux
        ninja -j$(nproc)

    - name: Run tests
      run: |
        cd src/build-linux
        ctest --output-on-failure

    - name: sccache stats
      if: always()
      run: sccache --show-stats

    - name: Build legacy.wad
      run: bash src/build_wad.sh src/release-linux/legacy.wad

    - name: Gather release files
      run: |
        mkdir -p src/release-linux
        cp src/build-linux/doomlegacy src/release-linux/

    - name: Package release
      run: |
        cd src
        tar -czf doomlegacy-linux.tar.gz -C release-linux .

    - name: Upload Linux release
      uses: actions/upload-artifact@v4
      with:
        name: doomlegacy-linux
        path: src/doomlegacy-linux.tar.gz
        retention-days: 30
        if-no-files-found: error
